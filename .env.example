# Copy to .env (or export directly). See src/config.ts.
CLAIMCHECK_DATABASE_URL=./claimcheck.db
CLAIMCHECK_PORT=8000
# CHANGE in production. Rotation requires re-issuing all API keys.
CLAIMCHECK_PEPPER=change-me-to-a-long-random-string
